# downloads

CI（`.github/workflows/docs.yml`）在构建文档站时生成：

- `rove-hop-routeros-bundle.zip` — 运维文档 + RouterOS 脚本（无容器镜像）

完整带镜像的部署包只在 **GitHub Releases**：

- `rove-hop-routeros-<version>-arm64.tar.gz`
- `rove-hop-routeros-<version>-amd64.tar.gz`
